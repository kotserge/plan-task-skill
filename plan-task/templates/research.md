# <Category> - Research: <Title>

## Goal of this pass

<The concrete after-state. Bullet what is true once this lands — these are the things `plan.md` must deliver.>

- <bullet>

Non-goals: <what this pass deliberately leaves alone.>

## Guiding principle (optional — delete if there isn't one)

<A one-line rule that the decisions below all serve. State it as a rule, then a sentence on what it implies concretely.>

## In scope / Out of scope

<What lands now vs what is deferred, each with a one-line reason. Tables read well here; bullets are fine for short lists.>

| Item | Why in scope |
|---|---|
| <item> | <reason> |

Deferred:

- <item> — <why deferred, and what would un-defer it>

## Open decisions

These need a call before `plan.md` can be concrete. Each states the question, lays out the options with their trade-offs, and ends with a recommendation. The user reviews and replies inline with `Note:` — leave the recommendation and the alternatives in place; the note records the decision and why.

### D1 — <the question, phrased as a choice>

<Context: what is true today that forces this choice.>

- **Option A — <short name>.** <trade-off.>
- **Option B — <short name>.** <trade-off.>
- **Option C — <short name>.** <trade-off — drop if only two.>

**Recommendation: <A/B/C>.** <Why — the reasoning that would survive the user disagreeing.>

Note: <left for the user to fill during review>

### D2 — <the question>

<Context.>

- **Option A — <name>.** <trade-off.>
- **Option B — <name>.** <trade-off.>

**Recommendation: <A/B>.** <why.>

Note:

## Sequencing notes / risk

<Dependencies between decisions, what must land first, and the riskiest step — the plan's `Executor:` choice for that step follows from it. Call out anything observable to downstream users — those changes are cheapest to make now.>

## What plan.md will contain

<Assuming the recommendations stand, an ordered preview of the steps. This becomes the skeleton of `plan.md` once the decisions are settled. Note any deferred decision (per its inline `Note:`) here so the plan's scope is unambiguous.>

1. <step preview>
2. <step preview>
