# Idea - Description: <Title>

> **Status: shelved.** Captured for future consideration; not actively planned. Promote
> to a `feature/N-<slug>/` folder (with `research.md` + `plan.md`) when a concrete
> consumer motivates the work. Until then it sits here as a known-known.

## Problem

<The use case that isn't served today, and why the current workarounds don't cover it.>

## What we want

<The capability, sketched. A motivating code/usage shape is welcome — mark it explicitly as non-binding ("without committing to it").>

## What stays the same

<Boundaries: the parts of the system this would not change. Reassures the future picker about blast radius.>

## Why this is shelved (and not yet a feature)

<The reasons it's parked — usually some mix of "no concrete consumer is asking yet" and "a design or dependency choice needs a real use case to anchor it." Designing in a vacuum ships the wrong feature set.>

## Open design questions (for research, when promoted)

<The architectural questions a future `research.md` would settle. Numbered, each phrased as a decision with the candidate directions.>

1. <question — candidate directions>
2. <question — candidate directions>

## Non-goals (even when promoted)

<What stays out of scope even after this graduates to a feature.>

## Pointers for the future picker

<Links to precedents in the codebase and related docs folders — where the relevant machinery lives, what prior feature established the pattern this would reuse.>
